import 'package:flutter/material.dart';

class PhoneFragment extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: const Center(
        child: Text('Phone Fragment'),
      )
    );
  }
}