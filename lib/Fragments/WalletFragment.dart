import 'package:flutter/material.dart';

class WalletFragment extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: const Center(
        child: Text('Wallet Fragment'),
      )
    );
  }
}